server:
  port: 8085
spring:
  application:
    name: CONFIG-DEV

  cloud:
    config:
      server:
        git:
          uri: https://github.com/DurgeshCoder/microservice-tutorial-config
          clone-on-start: true
